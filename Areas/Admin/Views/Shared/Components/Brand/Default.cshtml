@model IEnumerable<BrandModel>

@foreach (var brand in Model)
{
	<div class="panel panel-default">
		<div class="panel-heading">
			<h4 class="panel-title"><a asp-action="Index" asp-controller="Brand" asp-route-Slug="@brand.Slug">@brand.Name</a></h4>
		</div>
	</div>
}
